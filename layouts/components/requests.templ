package components

import (
	"gothstarter/database"
	"net/http"
)

templ Requests(user database.User, isAuth bool, r *http.Request) {
	@FeatureNavigation(isAuth, user.Username, r)
	<section class="bg-gray-200 w-screen h-screen py-8"></section>
}
