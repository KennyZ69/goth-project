package index

import (
	layouts "gothstarter/layouts/base"
)

templ Index(name string) {
	@layouts.Base() {
		<div class="flex flex-col flex-wrap px-5 mx-auto w-full py-3">
			This is { name }
		</div>
	}
}
