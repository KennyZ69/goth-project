package index

import (
	layouts "gothstarter/layouts/base"
	"net/http"
	// "gothstarter/layouts/components"
)

templ Index(r *http.Request, data bool, username string) {
	@layouts.Base(r, data, username)
}
